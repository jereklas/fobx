# Staged Docs

Anything under this directory is intentionally excluded from static site
generation.

Use it for docs that should be committed alongside in-progress package work but
must not publish yet.

To publish a page later, move it out of `_staged/` and into the normal
`content/` tree.